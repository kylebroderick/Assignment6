@model Book

<div class="card card-outline-primary m-1 p-1">
    <div class="bg-faded p-1">

        <h4 class="card-title">Title: @Model.Title</h4>
        <p class="card-subtitle mb-2 text-muted">Author: @Model.Author</p>
        <p>Publisher: @Model.Publisher</p>
        <p>ISBN: @Model.ISBN</p>
        <p>Classification/Category: @Model.Category</p>

        <span class="badge badge-pill badge-primary" style="float:right">
            <p>Price: @Model.Price</p>
        </span>

        <p>Pages: @Model.Pages</p>
    </div>

    <form id="@Model.BookId" asp-page="/Purchase" method="post">
        <input type="hidden" asp-for="BookId" />
        <input type="hidden" name="returnUrl" value="@ViewContext.HttpContext.Request.PathAndQuery()" />
        <span class="card-text p-1">
            @Model.Title
            <button type="submit" class="btn-success btn-sm pull-right" style="float:right">
                Purchase
            </button>

        </span>


    </form>





</div>